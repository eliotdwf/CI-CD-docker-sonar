name: tezstt

on:
    workflow_dispatch:
    # Trigger the workflow on push for the main branch
    # and pull request

jobs:
  echo-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Secret
        run: echo "The secret is ${{ secrets.SONAR_TOKEN }}"
